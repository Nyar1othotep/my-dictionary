import spinner from "../../resourses/img/Infinity-1s-200px.svg";

const Spinner = () => {
   return (
      <div className="spinner">
         <img src={spinner} alt="spinner" />
      </div>
   );
};

export default Spinner;
